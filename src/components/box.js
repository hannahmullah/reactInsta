function Box({ name }) {
    return (
        <div>
            <h1>{name}</h1>
        </div>
    )
}

export default Box;
//ALL COMPONENTS HAVE TO START WITH A CAPITAL LETTER

//you can only pass one parameter throuhg which is an object but you can have multiple fields within that object
//parameters must be in braces within the code as it is JS